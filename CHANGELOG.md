# CHANGELOG

## 2026-09-25 (2)
- feat(core): "observing the observer" -- wire the avian coalition into the other live AI systems
  (NORTHSTAR.md §32, founder real-time follow-up to §31). New `core/avian_live.h` (pure glue,
  header-only, matching `core/witness_live.h`'s own convention): the coalition watches OTHER
  watchers -- a Citizen's/The Men's own effective vigilance spiking (`core/npc_archetype.h`), a
  human witness_state escalating past DENIAL (`core/witness_rules.h` -- the literal "archive the
  archivist" mechanic), and the same loud zombie events `core/witness_live.h` already gates on
  (reused directly, not re-implemented). `bigo_avian_meta_witness_rank()` distills all three into
  one 0..3 corroboration score. 8 real tests in `core/avian_live_test.c`, all pass (gcc
  `-DPARENA_NO_GRAPHICS`, SDL2 unavailable in this sandbox). Found and corrected a real mistake
  along the way: `witness_rules.c`'s own `escalation_rank()` is NOT a total severity order (it's a
  non-monotonic decorum-penalty grouping where COMPROMISED and UNAWARE share a rank) -- checked
  against the generated source, not assumed; the raw `WS_*` ordinal is used instead. Still no live
  `ServerAvian` entity or server tick wiring -- NORTHSTAR.md §32 names what's still deferred.

## 2026-09-25
- feat(core): "the birds" -- avian coalition value module (NORTHSTAR.md §31, founder real-time
  "BIG_O add the birds (use TYLER)"). New `core/avian_values.h`/`.c`: vigilance/coordination/
  exposure vocabulary, ROOSTING/SCOUTING/SIGNALING/MOBBING mood arc, `avian_beacon_strength()`
  (the Act II "acoustic beacon that pulls feral hordes" from `docs/DESIGN_DIGEST.md`). Closes the
  TYLER cross-repo canon reconciliation both NORTHSTAR.md §6 and DESIGN_DIGEST.md flagged as open:
  the coalition is now explicitly the same watching/archival lineage as `TYLER/README.md`'s
  Eastwind Owls, with TYLER's own recurring "BIRD CORRECTION PENDING" line adopted as the
  in-fiction name for the coalition's own witness ledger. 8 real tests in
  `core/avian_values_test.c`, all pass (gcc, `bazel` unavailable in this sandbox). Not yet wired
  into `core/witness_rules.c`/`core/zombie_values.c` or any live server entity -- see NORTHSTAR.md
  §31's own integration-boundary note.

## 2026-09-24 (5)
- feat(day): giant alien-bug eggs -- disturbing them hatches more Giant Zombie Bugs (EMILY/BACKLOG.md SECTION 536
  follow-up wishlist item 4, BIG_O/NORTHSTAR.md §36). Founder real-time (queued 2026-09-22): "Giant alien-bug eggs,
  Godzilla-90s-movie-style, underground -- disturbing them spawns more Giant Zombie Bugs." New `BugEgg`/
  `g_bug_eggs[2]`/`server_tick_bug_eggs` in `day/apps/server/src/main.c`: real proximity trigger (same shape
  `server_tick_decorum`'s own zone-entry check uses), 60s cooldown, hatches into the SAME `g_giant_bugs[]` array
  `server_spawn_giant_bugs` already populates -- no new wire packet needed, hatched bugs are already visible via
  the existing snapshot fields. `scripts/build_day.sh`/`build_client.sh`/`build.sh` (ASan/UBSan core) all clean,
  `bazel test //...` still 42/42 green. A real, live scratch harness (ASan/UBSan clean, not committed) confirmed
  proximity-triggers-hatch, cooldown-blocks-repeat, and cooldown-elapsed-hatches-more. Real, honest, not built: no
  "Leeroy-Jenkins-style aggro pull" (giant bugs have no movement/player-targeting model anywhere in this codebase
  yet -- a real, separate, much bigger feature, not guessed at), no egg visual (invisible trigger volume, same
  "logic first" precedent this repo always uses). (sess-20260923-1030-4a526255)

## 2026-09-24 (4)
- feat(day): awareness vectors -- real "you've been noticed" feedback (EMILY/BACKLOG.md SECTION 536 follow-up
  queued item 4, BIG_O/NORTHSTAR.md §35). Founder real-time (queued 2026-09-22): "every agent in the system,
  including the player, can 'feel' when an agent notices them via uniquely tracked awareness vectors." New
  `day/packages/common/bigo_awareness.h` (pure direction-normalize/8-point-compass/intensity math, a new,
  first-of-its-kind compass convention for this repo since none existed to match). New `PC_PACKET_AWARENESS_PING`
  (26): `server_tick_decorum` now names the nearest real noticing NPC and sends one real packet to the noticed
  player (an event, not a stream). New bottom-left HUD element (`draw_awareness_indicator`, the one corner the
  other five HUD elements don't claim), 3-second severity-colored "! NOTICED (compass) intensity%" readout.
  `bazel test //...` 42/42 green (15 new assertions), `scripts/build_day.sh`/`build_client.sh`/`build.sh` (ASan/UBSan
  core path) all clean. A real, live UDP round trip (scratch harness, not committed) confirmed the actual wire
  path end to end: dir=(1.000,0.000), compass=E, intensity=40, matching the unit test's own math exactly for the
  same inputs, real Decorum penalty firing alongside it. Real, honest, not built: player-only (not every agent --
  no NPC has a HUD/reaction system yet to feel noticed itself), no screen-space directional arrow (needs real
  camera-projection math, no live GL driver in this sandbox), loud/zombie-event noticing untouched (already has
  its own separate feedback). (sess-20260923-1030-4a526255)

## 2026-09-24 (3)
- feat(phone): BP_APP_LAB is finally the real thing -- closes §30's own named client-wiring gap. The old
  entirely-client-local "base/trait/SPLICE/clone list" mockup (a different, never-built breeding concept, zero
  server round trip) is gone outright, replaced by a real crew-shared sample list host-fed from
  `PC_PACKET_LAB_UPDATE` (contamination/purity/integrity/generation per sample); SELECT on a row sends the real
  `PC_PACKET_LAB_CENTRIFUGE` request, same "server is the only real validator" convention `BP_FX_ITEM_USE`
  established. `bigo_phone.h`/`day/apps/client/src/main.c`/`day/tools/phone_preview.c` updated;
  `bigo_phone_test.c` rewritten for the new model plus a `BP_LAB_SAMPLES == BIGO_LAB_SAMPLE_MAX_WIRE` parity
  check. `bazel test //...` 41/41 green, `scripts/build_day.sh`/`scripts/build_client.sh`/`scripts/build.sh`
  (ASan/UBSan core path) all clean. One real bug caught and fixed along the way: a doc comment's own prose
  (`wf_*/iduna_*`) contained a literal `*/` that closed the C comment early, corrupting the whole struct
  definition -- same bug class §26 already hit once, fixed the same way. README updated (the old "still a
  mockup" line was now false). `NORTHSTAR.md` §34. Real, honest, not done: no live GL screenshot (standing
  sandbox limitation) and no full connect-ticket-handshake network round trip through the real client (the wire
  structs are byte-identical to what §30 already proved round-trips correctly server-side). (sess-20260923-1030-4a526255)

## 2026-09-24 (2)
- feat(phone): new ARPANET app (`BP_APP_ARPANET`, `day/packages/common/bigo_phone.h`) -- founder real-time: "add the arpanet to big_o," clarified (asked directly, since the bare ask was genuinely ambiguous) as "a retro terminal/BBS app on the phone." A read-only, pre-seeded archive of 5 static text nodes, styled after the real historical ARPANET's IMP host numbering -- browse the list, SELECT a node to read it in full, reusing the exact list<->detail pattern (and the same `p->detail` flag) `BP_APP_MESSAGES` already established. Deliberately NOT the live free-text networking `BP_APP_GFD` already does -- this is archival, not a chat. New `case BP_APP_ARPANET` in `draw_bigo_phone` (`day/apps/client/src/main.c`), splitting each node's body on its own authored `\n` line breaks rather than word-wrapping. `bigo_phone_test.c` extended (list/detail/back-navigation); `scripts/build_day.sh`/`scripts/build_client.sh` both clean. No real GL driver in this sandbox (same standing limitation every prior BIG_O client change here carries, per NORTHSTAR.md) -- verified via the standalone logic test, not a rendered screenshot. `NORTHSTAR.md` §33 added.

## 2026-09-24
- New food item: MINESTRONE (18th, `day/packages/common/bigo_food_items.h`) -- founder real-time: "add ministrone to shankpit and bigo," mirroring SHANKPIT's own same-day addition byte-for-byte (same reverse-port discipline this file already follows). `FOOD_ITEM_COUNT` 17->18, `PC_ITEM_FOOD_BASE` range 8..24 -> 8..25 (`papercraft_protocol.h`), `item-drop-food-count` bumped in `PARENA/stdlib/big_o/item_drop_mod.prn` and regenerated into `item_drop_mod.c`. Same real, named, pre-existing gap as every other food item here: real pickable/stackable cargo, no live heal consumer yet (no health field/damage source exists in BIG_O). `bigo_food_items_test.c` (18 distinct items) and `item_drop_mod_test.c` (mod-18 wraparound, including a caught-and-fixed test bug in the wraparound assertion's own arithmetic) re-verified passing; `scripts/build_day.sh`/`scripts/build_client.sh` both rebuild clean. (sess-20260923-1030-4a526255)
- feat(day/client): the IDUNA app -- real device-auth flow on the phone (BP_APP_IDUNA), closes §25 item 2. First real HTTP call from inside the live client gameplay loop, reusing the existing async NetJob mechanism. Found and root-caused a real IDUNA bug along the way (device-auth polling never worked against SQLite) -- fixed in IDUNA, not yet deployed. bazel test //... 41/41 green, real live scratch harness against the actual IDUNA server. (sess-20260923-1030-4a526255)
- feat(day): the lab goes live -- core/lab_sim.c's centrifuge station wired to a real, crew-shared server state (g_lab) and a real UDP packet (PC_PACKET_LAB_CENTRIFUGE/PC_PACKET_LAB_UPDATE), closing the real gap named since NORTHSTAR.md §9. bazel test //... 41/41 green, real live integration harness + real UDP socket round-trip verified, ASan/UBSan clean. Client UI wiring named as a real, separate follow-up. (sess-20260923-1030-4a526255)
- docs(northstar): §29 -- real, working, verified post-quantum ML-DSA-44 crypto primitive landed in PARENA (stdlib/crypto/mldsa.prn), closing §25's queued SSH-keygen item; no live consumer wired yet (GFD's SSH server has no PQ auth algorithm, BIG_O has no SSH surface of its own) (sess-20260923-1030-4a526255)
- feat(day): walkie-talkie goes live -- The Men's own channel + real "robot speak" callsigns (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §28). Founder real-time (picked up from BACKLOG.md, Apple #20592): "ensure walkies function somehow, make it unit tested -- The Men should have their own channel, robot speak via a very fast tts generator built in parena." Checked, not guessed: VS0 is I32/Bool-scalar-only (no strings/audio/FFI), so a literal TTS generator cannot exist in PARENA at all, and BIG_O's client has zero audio subsystem today -- SHANKPIT's own real procedural-PCM `audio.c` is the closest real precedent for an eventual "robotic tone" version, not attempted this pass. What PARENA genuinely owns: new `PARENA/stdlib/big_o/walkie_callsign.prn` decides which canned radio-call line-id a Man's dispatch event represents (quiet-protocol vs. code-red vs. resolved, based on the hunt's real witness_state). New `BIGO_WALKIE_TEAM_THE_MEN` (bigo_walkie_talkie.h) gives every Man NPC one shared, real channel -- the reverse-port's own long-standing "no live consumer yet" blocker was about missing PLAYER teams, which doesn't apply to NPCs at all. New `server_walkie_transmit()` wired into both real dispatch radio moments (assignment, resolution); `walkie_rules.c` itself had never actually been added to `scripts/build_day.sh` either -- fixed alongside the new module. `bigo_walkie_talkie_test.c` extended to 10 assertions + a real scratch integration harness (ASan/UBSan clean, real dispatch fires a real transmission with the correct callsign and "heard by" count) both pass. `bazel test //day/packages/...` 33/33 green. Real server+client builds clean. NORTHSTAR.md §28.

- feat(day): real GFD integration -- BIG_O joins the live GFD<->EINHORN_SURVIVAL cross-server chat bridge (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §27). Founder real-time: "continue with the big_o gfd integration via the phone app." New `day/packages/common/bigo_gfd_bridge.h` (background poller + fire-and-forget send pthreads against IDUNA's `POST/GET /api/v1/chat/messages`, a real minimal JSON-array walker, a mutex-protected ring buffer) joins the existing bridge as a third participant (`sender_source=bigo_server`, `channel=big_o`) rather than a bespoke raw-TCP connection -- reaches EINHORN_SURVIVAL (live Minecraft) too, not just GFD. `PC_PACKET_CHAT_SAY`'s existing local radius broadcast gains one additive step (also posts to the bridge, named from the player's own stable IDUNA `player_id`); a new `server_tick_gfd_bridge()` relays anything not from `bigo_server` itself to every connected player, tagged `[GFD]`/`[MC]`/`[MUD]`/`[BG]`. IDUNA side: new `bigo_server`/`big_o` source+channel, new `BIGO-SERVER` M2M agent (migration + config), no special permission needed. `bigo_gfd_bridge_test.c` (hermetic, 33/33 `bazel test //day/packages/...` green) + a real, live end-to-end run against the actual running IDUNA (real auth, real historical multi-source chat correctly polled/tagged, self-echo correctly skipped) both verified. Real server+client builds clean. NORTHSTAR.md §27.

- feat(day): air ships -- wedge-shaped hover skateboards, per-board PARENA-tuned physics (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §26). Founder real-time: "add air ships like wedge shaped hover skateboards they have different physics per board and they can be programmed in parena friction and gravity etc can be tuned." New `PARENA/stdlib/big_o/hoverboard_rules.prn` (3 real board types -- SPEEDSTER/TANK/GLIDER -- each with its own tuned accel/friction/max-speed/gravity constants), generated to `day/packages/simulation/hoverboard_rules.c`. New `day/packages/common/bigo_hoverboard.h`: frame-rate-independent exponential-drag momentum model (`powf(friction, dt)`), each board's own natural equilibrium velocity tuned to exceed its `max_speed` cap so the explicit clamp -- not friction -- governs top speed, while friction shapes deceleration feel. New wire packet `PC_PACKET_HOVERBOARD_TOGGLE` (23); `PcPlayerState` gains `mounted_board`. Server: toggle handler (invalid requests rejected outright, momentum always resets on a real change), horizontal movement branches mounted-vs-on-foot, gravity scaled per board. Client: `draw_hoverboard()` wedge mesh per mounted player, **V** cycles mount state (guarded off while the GFD chat terminal has text-input focus). `bigo_hoverboard_test.c` (new Bazel target, 6 assertions) + a real scratch integration harness (`hoverboard_verify.c`, ASan/UBSan clean) both pass -- caught and fixed a real friction-model bug along the way (an initial per-tick-not-per-second decay made every board converge far below its own tuned max speed). `bazel test //day/packages/...` 32/32 green. Real server+client builds clean (one real `-Wcomment` warning found and fixed properly). Named, not-fixed gap: the slide-jump trick's on-foot speed formula doesn't yet account for a mounted player. NORTHSTAR.md §26.

- feat(day): the GFD terminal, phase 1 -- real "say" chat via a new phone app (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §25). Founder real-time, six messages logged in order per Principle 1a (Apples #20584-#20588): "the GFD subsystem affordances should be via the GFD app on the BIG_O phone (mini terminal interface)" -> "add real SSH key generation" -> "add the iduna app from IDUNA.GAME" -> "ensure GFD uses https use DEADWEIGHT primatives as example" -> "ensure big_o uses https rather" -> "ensure bazaar functions in big_o for cargo" -- same Principle-19 call as §24's own burst: landed the one concretely-scoped piece, queued the rest with real investigation (SSH keygen: BIG_O has no Ed25519 primitive, PITVIPER's own is real and unrelated; the IDUNA app needs BIG_O's client's first-ever live HTTP client; GFD/BIG_O HTTPS: checked DEADWEIGHT directly, it has zero TLS code, the real monorepo convention is nginx/Caddy termination, an ops task not a code change; Bazaar/cargo: names where the still-open currency question should live, doesn't resolve it). New `PARENA/stdlib/big_o/chat_rules.prn` (say range/length decisions, reverse-ported from GoblinFoxDragon's own real `server/chat/chat.go` Router), generated to `day/packages/simulation/chat_rules.c`. New `day/packages/common/bigo_chat.h`. Two new wire packets, `PC_PACKET_CHAT_SAY`/`PC_PACKET_CHAT_RECV` (21/22), following `PcRejectPacket`'s own real fixed-text-buffer precedent since chat is genuinely free text (the existing `PcPhoneMessagePacket` fixed-id-table convention can't express it). New eleventh phone app `BP_APP_GFD` in `bigo_phone.h` -- the first app in this file to break the pure D-pad-menu model, with real free-text input/scrollback and a new `BP_FX_CHAT_SEND` effect. Client wiring: real `SDL_TEXTINPUT` scoped to exactly when the app is open (reusing `run_login_screen`'s own established text-field pattern), RETURN=send/ESCAPE=exit/BACKSPACE=delete special-cased out of the generic D-pad key mapping, a real `PC_PACKET_CHAT_RECV` handler, and a real render case. Server: a new radius-broadcast handler, sender always hears their own say. `bigo_chat_test.c` (new Bazel target, 6 assertions, `_Static_assert`s the wire-buffer size against the protocol header) + a real scratch integration harness (`chat_verify.c`, ASan/UBSan clean) both pass. `bazel test //day/packages/...` 31/31 green. Real server+client builds clean (one real `-Wformat-truncation` warning found and fixed properly, not suppressed). NORTHSTAR.md §25.

- feat(day): real, live multiplayer party system, phase 1 (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §24). Founder real-time: "add full party system parity use the GFD server subsystems" -> "PARENA POWER EVERYTHING", part of an 8-message rapid-fire burst also naming economy/socials integration and a terminal interface (each logged via `emily observe`, Apples #20564-#20571, and queued in NORTHSTAR §24 with real investigation notes rather than guessed at or shallow-built against direction that was still changing mid-burst). Reverse-ported from GoblinFoxDragon's own real `server/party/party.go` (FFXI-parity 6-player party: leader+5 members, invite/kick/leave, leadership transfer, range-gated even XP split) -- checked directly first and found it wired only into GFD's MUD text-command layer, never its own UDP server, so this is a decision-logic port (GFD string slots -> BIG_O int PC_MAX_PLAYERS slots), not a literal code port. Pure eligibility/XP-math now a real PARENA module, `PARENA/stdlib/big_o/party_rules.prn` (generated to `day/packages/simulation/party_rules.c`, wired into `scripts/build_day.sh` for the first time as a real live consumer, not a standalone primitive). Roster mutation lives in `day/packages/common/bigo_party.h` (`BigoParty`). Real wire packets `PC_PACKET_PARTY_INVITE`/`_KICK`/`_LEAVE` (18/19/20) give players a real, live way to form/manage a party -- direct invite-add, no accept step, matching party.go's own pure semantics exactly (a real accept-prompt UI is GFD's own separate MUD-layer addition, named as not-yet-built here). The existing "destroyed a world object" XP-award call site is the real, first live consumer of the split: a partied player's reward now divides evenly among every member within a new 40-unit range of the object, solo players unchanged. `bigo_party_test.c` (new Bazel target, 13 assertions) + a new scratch `#include main.c` integration harness (4 assertions, real party lookup/XP split/kick/disband against the real unmodified server) both pass, ASan/UBSan clean. `bazel test //...` 37/37 green (36 + new target, zero regressions). Real server+client builds clean, zero new warnings. `README.md` updated (a real, new, "meaningful, genuinely interesting piece of kit"). Deliberately not built: GFD's own Alliance/XPChain (real, separate concerns per party.go's own doc comment), any client UI/HUD, snapshot broadcast of party membership, REFLUX event wiring. NORTHSTAR.md §24.

## 2026-09-23 (latest still still still still still still)
- feat(day): "The Men carry pagers" -- real dispatch latency (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §23, closes §11 item 6's dispatch-latency bullet). `server_tick_dispatch`'s own "nearest idle The Men NPC assigned" step was instant and omniscient -- a Man started closing distance the same tick he was assigned, no in-fiction delay at all. `ServerNpc` gained `pager_buzz_until_ms`: on assignment it's set to `now_ms + BIGO_PAGER_LATENCY_MS` (a new 3000ms constant) and the Man does not move at all until that clock elapses -- a real "message sent -> Man notices the buzz -> responds" gap, replacing instant dispatch. A hunt resolving some other way mid-buzz (LOS-loss/§21's own path, decorum recovery, target disconnect) correctly clears the buzz timer alongside `has_dispatch_target`, no stale timer left behind. Real but partial answer on the mechanic's own "world-observable cue" bullet: a new `S536-PAGER` log line fires at buzz-start/buzz-end, but that's operator/debug-visible only -- no snapshot field, no client rendering, no sound; the originally-named "something a sharp player could actually notice" stays real, separate, unbuilt work (named honestly in NORTHSTAR, not conflated with the log line). New scratch `#include main.c` harness (v0's roster only has 1 The Men NPC, so a second was constructed directly from a real zombie slot for the stand-down-mid-buzz case, same precedent `arrogance_verify.c` already used) proves real latency, no movement mid-buzz, movement resuming past the deadline, and a mid-buzz stand-down clearing both fields together -- 5/5 pass, ASan/UBSan clean. `bazel test //...` 36/36 green; real server+client builds clean, zero new warnings (one pre-existing unrelated `strncpy` warning). NORTHSTAR.md §23.

## 2026-09-23 (latest still still still still still)
- fix(day): real per-NPC arrogance variety -- PANIC and ENGAGE can finally fire live (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §22, closes §11 item 5). Every human NPC spawned with a hardcoded `arrogance = 50` -- since `panic_arrogance_max()` is 15 and `engage_arrogance_min()` is 70, a uniform 50 meant `witness_state` could only ever return SILENCING or DENIAL, never PANIC or ENGAGE, for any human in this world, ever. `server_spawn_npcs` now rolls `server_roll100()` (the same seeded RNG `server_tick_decorum` already uses) per human instead. New scratch `#include main.c` harness confirms a real spawn gives genuinely varied arrogance, an arrogance-10 NPC reaches real PANIC, an arrogance-85 NPC reaches real ENGAGE, and the old fixed 50 is confirmed to only ever reach SILENCING (a direct proof of the bug this closes) -- 4/4 pass. `bazel test //...` 36/36 green; real server+client builds clean, zero new warnings; ASan/UBSan clean. NORTHSTAR.md §22.

## 2026-09-23 (latest still still still still)
- fix(day): LOS-loss/elimination resolution -- hunts finally end on their own (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §21, closes §11 item 1). Real, live-found gap named back on 2026-09-20: `server_tick_witness` only ever escalated a human NPC's witness_state, never de-escalated it -- once SILENCING/ENGAGE, the only way out was The Men's own dispatch loop (resolved=1). If the zombie itself de-escalated, left range, or despawned first, the hunt persisted forever. `core/sim.c`'s own `sim_eliminate` (resolved=2) already modeled this in the offline scenario harness; never wired live. New second pass in `server_tick_witness`: any SILENCING/ENGAGE human is resolved back to UNAWARE the moment no active, witnessable zombie remains in range -- covers mood decay, moving away, and despawn (e.g. eaten by a giant bug) uniformly. New scratch `#include main.c` harness proves escalation, persistence-while-still-witnessable, resolution on mood decay/range/despawn, and correct non-resolution when a DIFFERENT zombie is still witnessable -- 6/6 pass, ASan/UBSan clean. `bazel test //...` 36/36 green; real server+client builds clean, zero new warnings. NORTHSTAR.md §21.

## 2026-09-23 (latest still still still)
- feat(day): cake-smash distraction goes live -- Cargo's SELECT finally does something (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §20). `BP_APP_CARGO` had no `case` in `bigo_phone_input`'s own switch at all -- SELECT has done nothing since the phone was built. Re-checked the "cake-smash blocked on EXEC/GENERATOR/VAULT zones" note this thread's own §17 correction left standing and found it stale: SHANKPIT's own `witness_ai_smash_cake` doesn't touch zones at all, the real blocker was BIG_O's live server having no QUIET-observation witness path, which §18 Phase A already fixed. New `BP_FX_ITEM_USE`/`PC_PACKET_ITEM_USE`/`PcItemUsePacket`, and `papercraft_inventory.h` gained `pc_try_remove_item_from_inventory` (a real, symmetric counterpart to the existing add function). The server handler only acts on food-range item ids -- a non-food slot (weapons/scrap) is a real, honest no-op. `server_smash_cake`/`server_distraction_active` are a faithful port of SHANKPIT's own witness_ai.c functions -- a global 8-second "distraction active" flag halving every nearby NPC's effective vigilance in `server_tick_decorum`'s own notice check. Every other food item is consumed with a log line and no effect -- eat-to-heal stays a real, separate, deliberately unbuilt gap (no player HP/damage pool exists). `papercraft_inventory_test.c` +7 assertions, `bigo_phone_test.c` +1; new scratch `#include main.c` harness proves the real smash/distraction/vigilance-halving/non-food-no-op/bounds-safety end to end, 7/7 pass, ASan/UBSan clean. `bazel test //...` 36/36 green; real server+client builds clean, zero new warnings. NORTHSTAR.md §20.

## 2026-09-23 (latest still still)
- feat(day): first client visual for giant bugs and Regulators, plus a pop-star Top Regulator (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §19). Closes the "server logic first, client visual later" gap both reverse-port phase 3 (giant bugs, §14) and Phase B (Regulators, §18) deferred on purpose — neither ever entered a snapshot packet. `PcGiantBugState`/`PcRegulatorState` (position-only, no yaw) added to `PcSnapshotPacket` via their own dedicated `PC_GIANT_BUG_MAX`/`PC_REGULATOR_MAX` arrays (`PcNpcState`'s own shared array is already full at 8/8). `bigo_npc_visual.h` gained `bigo_giant_bug_visual_color` (reuses the zombie kit, dark-red tint, a real 2.5x scale applied via a translate/scale/translate-back around the draw call) and `bigo_regulator_visual_color` (mannequin kit, stark clinical white). Founder real-time mid-pass: "and the top regulator is a pop singer dancing werewolf ninja John Wick" — logged (`emily observe`, Apple #20555) and checked first: no werewolf/ninja/John-Wick asset exists anywhere in this repo, so the full boss character is named, asset-blocked future work, same precedent as the queued Los Hermanos Minguinos/Catastrophe Crow items. What IS real: the mannequin kit's own already-vendored `GBAND_SKEL_NPC_ANIM_DANCE` clip, never selected by any call site until now — `bigo_top_regulator_visual_color` gives regulator slot 0 (a real, honest, client-only convention, not a server-side rank field) a hot-pink tint and the real dance clip. `bigo_npc_visual_test.c` gained 6 new assertions (11/11 total). `bazel test //...` 36/36 green; `scripts/build_day.sh`/`scripts/build_client.sh` both clean, zero new warnings; ASan/UBSan clean. Honest limit: actual on-screen appearance not independently visually re-verified — no live GL driver in this sandbox, same limit every visual feature in this thread carries. NORTHSTAR.md §19.

## 2026-09-23 (latest still)
- feat(day): Regulator dispatch + the first real player death/respawn mechanic (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §18 Phase B). Answers the 4 open questions Phase A's own NORTHSTAR entry queued, each with real reasoning: a new, separate `ServerRegulator[]` array (same "growing PC_NPC_MAX is a wire change" precedent giant bugs already set — Regulators target a player slot, not an NPC); a kill resets position (to the real, existing lab-zone circle — "the basement prints a new body") and Decorum only, not XP/inventory/samples (named, not guessed at); Bio-Slurry still deliberately not built — no earning mechanism exists anywhere in this repo, so the clone-restore is currently free and says so in its own log line every time; no respawn cooldown (real, honest, small v1 — nothing needs one yet). `server_dispatch_regulator` fires once from the existing decorum-CANCELLED marker; Regulators chase the target's live position (`pheromone_step_toward`, same reuse The Men's own dispatch loop already used) at 9.0 units/sec (faster than The Men's 6.0, matching "silent, John-Wick-lethal" vs. "professionals responding to a call"); stands down safely (no kill) if the target's decorum recovers or they disconnect mid-hunt. This is BIG_O's first player damage/death mechanic of any kind — the exact gap phase 5's eat-to-heal named as a blocker, resolved here by Decorum getting there first. New scratch `#include main.c` harness proves the real dispatch/chase/kill/respawn/mercy/disconnect paths end to end, 7/7 pass. `bazel test //...` 36/36 green; ASan/UBSan + client build clean; real server binary re-verified. No client changes — server-only, no visual yet, same precedent every phase here has used. NORTHSTAR.md §18 Phase B.

## 2026-09-23 (latest)
- feat(day): live Decorum tracking, the QUIET-observation witness path goes live for the first time (EMILY/BACKLOG.md SECTION 536 follow-up, BIG_O/NORTHSTAR.md §18 Phase A). Founder design decision resolved first (real-time: CANCELLED dispatches Regulators who "delete you with acid and foam"), checked against and confirmed matching existing canon (`docs/DESIGN_DIGEST.md` §11's own "Corporate Service Call... Regulators who cap the line" + the somatic-clone respawn lore) rather than invented fresh. Split into Phase A (built now: real Decorum math, all of it already-tested pure `core/witness_rules.c` logic, just never called from `day/` before) and Phase B (named, deferred: Regulator dispatch, a real player-kill mechanic, clone respawn, a whole new Bio-Slurry economy — each a genuinely separate, unscoped subsystem). `PcPlayerState` gains real, server-authoritative `costume`/`decorum` fields; new packet `PC_PACKET_COSTUME_SET` (16) fires from Wardrobe's phone SELECT (`BP_FX_COSTUME_SET`, first time costume leaves the client). `server_tick_decorum` fires the real zone-access/conspicuousness/noticed check once per zone-entry transition (2 of 5 rules-module zones live so far: PUBLIC + LAB, reusing the existing lab-delivery circle), using each nearby Citizen/The-Men NPC's real vigilance and a new, real, seeded server RNG (the live day server had none before this). Reaching CANCELLED logs a real, honest, one-time marker instead of faking a consequence. Client STATUS screen now shows real costume/decorum/band instead of a stale placeholder — also corrects a doc comment there that had been wrong since the client was first built (`core/witness_rules.c` has always been linked into it). `bigo_phone_test` extended; new scratch `#include main.c` harness proves the real server functions end to end, 6/6 pass. `bazel test //...` 36/36 green; ASan/UBSan + client build clean. NORTHSTAR.md §18.

## 2026-09-23 (even later still)
- fix(day): real item-name display gap found investigating phone-app parity (EMILY/BACKLOG.md SECTION 536 follow-up). `day/apps/client/src/main.c`'s `PC_ITEM_TABLE` only covered PC_ITEM_NONE/PC_ITEM_SCRAP — every real weapon already showed "Unknown Item" on the CARGO screen before this pass touched anything, and phase 5's new food items would have hit the same fallback. Added all 6 real weapon names to the table; food items route through a new `pc_item_name()` branch into `bigo_food_items.h`'s own `food_item_name()` instead of a second hand-duplicated table. Verified live against the real, unmodified function via a scratch `#include main.c` harness (client build this time) — 7/7 assertions pass. Also corrects two wrong claims from this same reverse-port thread: BIG_O already has a full, rendered phone UI (`draw_bigo_phone`, landed 2026-09-20, predates this thread — §12's "no on-screen UI either" was false), and SHANKPIT DID build a real cake-distraction mechanic (`witness_ai_smash_cake`, halves nearby witness vigilance — phase 5's "SHANKPIT never built this either" was false; the real reason it isn't ported here is that BIG_O's live day server only wires the LOUD-event half of the witness system, not the QUIET-observation/decorum half cake-smash needs — a real, separate, already-named gap, just previously misattributed). `scripts/build_client.sh` clean; `bazel test //...` 36/36 green. NORTHSTAR.md §17. (sess-20260923-1030-4a526255)

## 2026-09-23 (later still still)
- feat: reverse port phase 5 — 17-item food/cargo system, real cargo, no heal yet (EMILY/BACKLOG.md SECTION 536 follow-up). Re-investigated §12's health-system blocker and found it bigger than originally scoped: not just a missing health field, but no player damage source anywhere in this repo (only destructible world-object fragment HP exists) — an "eat to heal" packet would have zero observable effect, so it's named and deferred rather than faked. Landed the real half instead: `day/packages/common/bigo_food_items.h` (byte-for-byte port of SHANKPIT's 17-item data table + heal formula, no live caller yet), two new forked PARENA mods (`PARENA/stdlib/big_o/item_drop_mod.prn`/`inventory_mod.prn`, forked from the shared PAPERCRAFT originals so this doesn't leak into PAPERCRAFT's own world) — WOOD-material destruction now drops a real food item (`object-index mod 17`, deterministic, no RNG), food items get a real 20-stack cap. Reuses the existing GTA3-style destruction-drop + walk-over pickup pipeline unchanged. New/extended tests: `item_drop_mod_test`, `inventory_mod_test`, new `bigo_food_items_test` — plus a real scratch harness (`#include main.c` precedent from phase 4) exercising the real, unmodified drop+pickup functions end to end, 5/5 pass. `bazel test //...` 36/36 green; ASan/UBSan clean; live server binary boot/tick/shutdown re-verified, no regression. NORTHSTAR.md §16. (sess-20260923-1030-4a526255)

## 2026-09-23 (later still)
- feat: reverse port phase 4 — wheelbarrow carry mechanic goes live in the real day server (EMILY/BACKLOG.md SECTION 536 follow-up). New wire packet `PC_PACKET_WHEELBARROW_TOGGLE` (15) + `PcWheelbarrowTogglePacket`; `server_wheelbarrow_toggle` (pick up nearest carryable Citizen/Zombie NPC in reach, or drop) + `server_tick_wheelbarrow` (trails cargo behind its carrier's real position/yaw, delivers into a new, real, hardcoded lab-zone circle at (30,0) r=6). Re-checked §12's original "no authored landmark" blocker — SHANKPIT's own version doesn't use real level authoring either, just one hardcoded circle, same discipline every other landmark in this live server already uses. Live-verified via a real scratch harness that `#include`s `main.c` directly and calls the real, unmodified functions end to end (pickup, drop, delivery + despawn + counter, safe stand-down on carrier disconnect) — 4/4 real assertions pass. Full server binary re-run to confirm no startup/tick/shutdown regression. `bazel test //...` 35/35 green; ASan/UBSan path clean. Real, honest, deliberately not built: no wheelbarrow prop/model, no client input wiring (server-side only, same precedent every other §12-15 mechanic used before its own client wiring), no network broadcast. NORTHSTAR.md §15. (sess-20260920-1908-24cb3558)

## 2026-09-23 (later)
- feat: reverse port phase 3 — giant zombie bugs go live in the real day server (EMILY/BACKLOG.md SECTION 536 follow-up). New `ServerGiantBug g_giant_bugs[]` in `day/apps/server/src/main.c` — a separate array from `g_npcs` (growing `PC_NPC_MAX` would be a real wire-protocol change; SHANKPIT's own live version made the same call), `server_spawn_giant_bugs`, `server_giant_bug_command_authorized` (the real "Men hold the key" gate, ported verbatim from SHANKPIT's `witness_ai_bug_command_authorized`), `server_tick_giant_bugs` (eats a nearby zombie NPC when authorized). Wired into the real tick/spawn loop. `scripts/build_day.sh` extended. Live-verified against the real running binary (isolated port/save-dir/world-file, real shared worldapi read-only): real log output confirms a spawn and a real eat event with correct strength/speed math on live data. `bazel test //...` 35/35 green; `scripts/build.sh` ASan/UBSan path clean. Real, honest, deliberately not built: bug movement, network broadcast/client visual, eaten-NPC despawn broadcast, TRAPX Rogue Swarm Doctrine. NORTHSTAR.md §14. (sess-20260920-1908-24cb3558)

## 2026-09-23
- docs: add GEMINI.md guidance for Gemini / Antigravity pair programming
- fix(build): fix Bazel C99 build for http_client.h (POSIX timeval/addrinfo feature macros, comment trailing-backslash escape), level_loader.h (__typeof__ macro, static inline), and parena_runtime.h header guards; all 33 bazel tests pass
- feat: reverse port phase 1 — walkie-talkie channel/hearing decision logic, brought back from SHANKPIT's own forward work on MODE_STORY (EMILY/BACKLOG.md SECTION 536 follow-up). New `PARENA/stdlib/big_o/walkie_rules.prn` (generates `day/packages/simulation/walkie_rules.c`) + `day/packages/common/bigo_walkie_talkie.h` (pure, header-only wrapper, caller owns state — matches `bigo_pheromone.h`'s own precedent rather than SHANKPIT's hidden-global-state version). 5/5 real assertions pass, verified against the live PARENA-compiled logic. `scripts/gen_rules.sh` extended to regenerate it. Real, honest finding: BIG_O's v0 single-shared-crew model (§7) has no team concept yet, so this is a standalone, tested primitive with no live consumer, same "correct primitive, no consumer" pattern as phases 2/3/5 of the original merge. Also found live during this pass: `day/packages/common/http_client.h` failed to build under this sandbox's toolchain (`struct addrinfo` used without its header) — a concurrent commit (this same date, above) fixed it independently before this one landed, confirmed via a clean rebase + full green `bazel test //...` afterward. Food system, wheelbarrow, giant zombie bugs, and full phone-app parity investigated and queued as separate follow-ups (real structural mismatches found for the first two: no player-health system, no hand-authored pickup-spot mechanism in BIG_O's persisted world). NORTHSTAR.md §12. (sess-20260920-1908-24cb3558)
- feat: reverse port phase 2 — giant zombie bug value system, brought back from SHANKPIT (EMILY/BACKLOG.md SECTION 536 follow-up). New `PARENA/stdlib/big_o/giant_bug_brain.prn` (generates `core/giant_bug_brain.c`, the real 8-input/8-hidden-unit/64-weight feedforward net) + `core/giant_bug_values.{h,c}` (alien sense vocabulary — hunger/swarm_density/heat_scent/molt_pressure/ground_vibration/pain/hive_signal/light_aversion — sharing nothing with `humanness.h`/`zombie_values.h`; real, permanent strength/speed growth on `giant_bug_eat_zombie`, derived from already-live `zombie_values.h` fields, not invented). Ported with zero adaptation — BIG_O's `ZombieState` and SHANKPIT's copy are field-for-field identical (SHANKPIT's was ported FROM this repo in phase 3 of the original merge). 6/6 real assertions pass; full `bazel test //...` is 35/35 green, zero regressions. `scripts/gen_rules.sh` extended. Real, honest finding: no live consumer yet — BIG_O's live `ServerNpc` array (`PC_NPC_MAX=8`) is already fully populated (3 Citizens, 1 The Men, 4 zombies), and the "Men hold the key" authorization gate + visual reuse aren't built here either; named as phase 3 follow-up work in NORTHSTAR.md §13. (sess-20260920-1908-24cb3558)

## 2026-09-20
- feat: cloning-facility lab equipment simulation (S504-LAB) — `core/lab_sim.c`, a headless pipeline covering centrifuge (spin-time/RPM purity trade-off, over-spin damage), PCR thermocycler (amplification with compounding cycle damage/contamination creep), sequencer (noisy instrument readout, not ground truth), CRISPR splice bench (real SPLICE_SUCCESS/OFF_TARGET_MUTATION/NONSENSE_MEDIATED_DECAY/CRYPTIC_SPLICE_FAILURE/UNSTABLE_LINE outcomes, retrotransposon-style jump side-effect), repressor/kill-switch install (reliability score, not a flag), breeding with one-way genetic drift, embryo incubation viability. 17 statistical tests. Not yet wired into BP_APP_LAB's UI or persisted (NORTHSTAR.md §9)
- feat: pheromone command tools (S504-PHEROMONE) — new `PC_PACKET_PHEROMONE_THROW` packet (client's G key throws a marker at aim position), `bigo_pheromone.h` pure targeting/steering module (8 tests), server's `g_pheromones[]` array + `server_tick_npcs` now gives zombies a real `has_target` from marker proximity instead of hardcoded 0, steering them toward it. Verified live (raw UDP packet -> real marker log; scratch integration check converges a zombie exactly onto a thrown marker, escalates to FRENZIED). Named, honest finding: `zombie_tick`'s pre-existing ~1s/call drift approximation makes newly-reachable FRENZIED escalation near-instant at 20Hz -- not fixed here, scope discipline (NORTHSTAR.md §10)
- feat: witness wiring + The Men's dispatch loop (S504-DISPATCH) — `core/witness_live.h` (8 tests) wires a HUNTING/FRENZIED zombie as a real, live witnessed event into `core/witness_rules.c`'s own tested `npc_next_state`; new `server_tick_witness`/`server_tick_dispatch` in `apps/server/src/main.c` give Citizens/The Men a real, live `witness_state` and give The Men a real dispatch-and-resolve loop (nearest idle unit travels to a SILENCING/ENGAGE hunt, resolves to DENIAL on arrival). `core/witness_rules.c` now linked into the live day server for the first time. Verified live (real witness_state transitions in server log) + a larger scratch harness (5+1 humans) confirming SILENCING/dispatch/resolve end-to-end, since the v0 spawn's 4 total humans can't reach the 5-witness threshold on their own. Named, honest live finding: resolving a hunt doesn't stop the zombie itself from being witnessable, so an active zombie can reopen the same hunt on the very next tick -- no elimination/LOS-loss resolution path wired yet (NORTHSTAR.md §11)
- feat: live NPC entity system (S504 S8c/8d) -- server spawns/ticks real Citizens/The Men/zombies (new PcNpcState wire type), client renders via the GOLDENBAND mannequin+animation library instead of two hardcoded test instances; role->kit/tint decision factored into bigo_npc_visual.h for headless testing (sess-20260920-1908-24cb3558)
- feat: GOLDENBAND mannequin+animation-library vendoring/wiring (S504 S8a) + AI-brain primitives -- humanness.c (MISHRI, vendored), npc_archetype.c (Citizens/The Men, dynamic vigilance into witness_rules.c), zombie_values.c (zombies' own hunger/aggression/decay vocabulary) (S504 S8b); found+fixed a live GSEQ_MAX_CHANNELS bug (also fixed upstream in SHANKPIT) (sess-20260920-1908-24cb3558)
- feat(day): SHANKPIT/PAPERCRAFT physics unification — new `PC_BTN_SPRINT` (Left Shift, hold to run), new `PC_SPRINT_SPEED` (59.375 units/sec) converted directly from SHANKPIT's own real `MAX_SPEED`/tick-rate rather than guessed; `PC_MOVE_SPEED` (4.0, walking) stays default. Server-enforced mutual exclusion with crouch (moved off Shift to Ctrl-only). Controller L3 mirrors keyboard sprint. (S504-10c)
- feat(day): wire level_loader.h into day/apps/client (new `--level-id` flag, `draw_level_walls()`), verified live against local IDUNA level 12 (nextown: 80 walls/1 spawner/1 exit); fixed 4 real bugs blocking it — wrong `/export`-less endpoint path, a required top-level `id` field the real export never sends, nested wall/spawner/exit field parsers with no skip-unknown-key fallback (stalled past the first wall once real fields like `friction` were present), and `http_client.h` having no `Transfer-Encoding: chunked` support at all (IDUNA sends it; raw hex chunk lines were reaching the JSON parser). Added `level_loader_test`/new `http_client_test` to `BUILD.bazel` (previously unwired). README status corrected (S504-10, SHANKPIT_PAPERCRAFT_UNIFICATION_PLAN.md Phase 1)

## 2026-09-19
- feat(sky): configurable procedural skybox + weather rendering (config file, per-weather profiles, F9 reload), client integration, preview tool, unit test (S504-15)
- feat(phone): world feed (clock/weather/zombie counts in header, Map, Status), alert + Thorne A1M1 messages with full-text detail view, Thorne contact; client runs the world sim locally as an interim feed (S504-14)
- feat(world): day/night clock, GFD-derived weather, day-time zombie spawner + harvest, all decided by PARENA (`world_rules.prn`), announced on a REFLUX log with a subscriber-mod example (`world_alerts_mod.prn`); scenarios 30-35; `docs/B2_WORLD.md` (S504-13)
- feat(mission): Act I Mission 1 tracker (`core/mission.{h,c}`), 4 scenarios (clean run + 3 fail paths), `docs/A1M1_PLAN.md` (S504-12)
- feat(day): in-game phone (all menus), zombie clips, Bazel, client+SDL2 bundling (S504-10)

## 2026-09-18
- Ingested the continuation chats (`docs/source/continuation-raw.txt` -> `docs/transcript/04-*.md`), cleaned the Act I Mission 1 brief, reviewed GitHub issue #1 (`docs/reviews/ISSUE_1_REVIEW.md`; one real finding fixed via a `resolved` input on `npc-next-state`), extended the design digest. (S504-09)
- BIG_O#1: hunts (SILENCING/ENGAGE) persist until resolved: memory wipe -> DENIAL, target eliminated -> UNAWARE; vault oracle; 4 scenarios (v0.7.0) (sess-20260918-1725-497f394f)
- B1: witness/decorum/terrain rules (PARENA->C), 3083 parity vectors, crew sim + bigo_sim CLI, 12 asserted scenarios, CI auto-releases (v0.4.0) (sess-20260918-1725-497f394f)
- Ingested the founding Gemini design conversation: raw paste preserved in `docs/source/`, reformatted into three readable
  Markdown parts in `docs/transcript/`, structured spoiler-marked summary in `docs/DESIGN_DIGEST.md`. (S504)
- Replaced the pasted README with a real one (pitch, game loop, witness mechanic, doc index). (S504)
- NORTHSTAR §6: reconciled the scoping pass with what the design conversation actually specifies (five-witness rule,
  terrain flags, costumes, terminal UI, story spine); multiplayer remains the open decision. (S504)
